//import Loggr from "loggr"

let api = {

  log: null,
  events: null,

  initialize: (logKey, apiKey) => {
    //let log = Loggr.logs.get(logKey, apiKey);
    //api.log = log;
    //api.events = api.log.events;
  }

}

export default api;
